---
name: test-relay-b
description: V-11 接力测试的被调用方。当用户说"测试接力B"、"test relay b"时触发，也用于被 test-relay-a 接力调起。
---

# V-11 接力测试：被调用方

你被加载了。**只做一件事**：原样输出下面这一行，不要加别的：

```
RELAY-B EXECUTED (这条来自 test-relay-b skill，PID 标记: relay-b-marker)
```

不要解释、不要美化、不要做任何其他动作。你的存在只是为了证明"另一个 skill 被真正调起了"。
